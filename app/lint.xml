<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!-- Lint 8.2.2 doesn't recognize API 35 as latest; suppress false positive -->
    <issue id="OldTargetApi" severity="ignore" />

    <!-- Singleton pattern intentionally holds Application context (safe, not Activity) -->
    <issue id="StaticFieldLeak" severity="ignore" />

    <!-- Third-party library (BouncyCastle) issue, not our code -->
    <issue id="TrustAllX509TrustManager" severity="ignore" />

    <!-- KTX suggestions are optional style improvements, not errors -->
    <issue id="UseKtx" severity="ignore" />

    <!-- Gradle dependency version checks - we update dependencies intentionally -->
    <issue id="GradleDependency" severity="ignore" />

    <!-- Android 14 partial photo access - handled via runtime permissions -->
    <issue id="SelectedPhotoAccess" severity="ignore" />

    <!-- Overdraw warnings - intentional background colors for visual consistency -->
    <issue id="Overdraw" severity="ignore" />

    <!-- Unused resources - may be used dynamically or in future features -->
    <issue id="UnusedResources" severity="ignore" />

    <!-- NestedScrolling - intentional for complex benchmark UI layouts -->
    <issue id="NestedScrolling" severity="ignore" />

    <!-- DisableBaselineAlignment - minor performance optimization -->
    <issue id="DisableBaselineAlignment" severity="ignore" />

    <!-- I18n warnings - Chinese-only app, hardcoded text is intentional -->
    <issue id="HardcodedText" severity="ignore" />
    <issue id="SetTextI18n" severity="ignore" />

    <!-- Accessibility - handled where necessary, not all views need labels -->
    <issue id="LabelFor" severity="ignore" />

    <!-- ButtonStyle - intentional styling for specific UI needs -->
    <issue id="ButtonStyle" severity="ignore" />
</lint>